CREATE DATABASE subscribeDB;

CREATE TABLE users (
    id INT NOT NULL AUTO_INCREMENT,
    email varchar(50) NOT NULL,
    domain varchar(50) NOT NULL,
    TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY(id)
) 